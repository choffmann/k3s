apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-{{ .Values.appName }}
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: cedrik.hoffmann@jgdperl.com
    privateKeySecretRef:
      name: letsencrypt-{{ .Values.appName }}
    solvers:
      - http01:
          ingress:
            class: traefik
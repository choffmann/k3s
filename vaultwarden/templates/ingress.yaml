apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.appName }}-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: letsencrypt-{{ .Values.appName }}
spec:
  ingressClassName: traefik
  rules:
    - host: {{ .Values.url }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.appName }}-service
                port:
                  number: 80
          - path: /notifications/hub
            pathType: Exact
            backend:
              service:
                name: {{ .Values.appName }}-service
                port:
                  number: 3012
  tls:
    - hosts:
        - {{ .Values.url }}
      secretName: {{ .Values.appName }}-tls